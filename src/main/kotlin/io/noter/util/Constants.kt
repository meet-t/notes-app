package io.noter.util

class Constants {

    companion object {
        const val BEARER = "Bearer"
        const val USERID_ATTRIBUTE = "userId"
        const val EMAIL_ATTRIBUTE = "email"
        const val NAME_ATTRIBUTE = "name"
        const val INVALID_TOKEN = "INVALID_TOKEN"
    }
}